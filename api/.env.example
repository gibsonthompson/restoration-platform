# API backend env. Copy to .env and fill in. NEVER commit .env.
PORT=8787
# Server-side only. Keeps the key off the client.
ANTHROPIC_API_KEY=
# Service-role key for server-side report/storage work (NEVER ship to frontend).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
